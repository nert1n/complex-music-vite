export const Loader = () => {
	return (
		<div className="flex min-h-screen items-center justify-center">
			<div className="inline-flex w-20 items-center justify-between">
				<span className="h-3 w-3 rounded-full bg-gray-400 animate-bounce [animation-delay:-0.32s]" />
				<span className="h-3 w-3 rounded-full bg-gray-400 animate-bounce [animation-delay:-0.16s]" />
				<span className="h-3 w-3 rounded-full bg-gray-400 animate-bounce" />
			</div>
		</div>
	);
};
